import A from "./types/tagstree";

/**
 * Takes a question and return an answer
 * @param {Question} question 
 * @param {import("./types/tagstree").Tag} tag
 */
function answer(question, tag) {
   return 42;
}

const t = new A();