import { AddIntervalDirective } from './add-interval.directive';

describe('AddIntervalDirective', () => {
  it('should create an instance', () => {
    const directive = new AddIntervalDirective();
    expect(directive).toBeTruthy();
  });
});
